spring:
  # Database configuration for development
  datasource:
    url: jdbc:h2:file:./data/mobilebanking;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password: dev_password_only

  # JPA configuration for development
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  # H2 Console configuration
  h2:
    console:
      enabled: true
      settings:
        web-allow-others: false

  # Flyway configuration for development
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    clean-on-validation-error: false

# Logging configuration for development
logging:
  level:
    com.mobilebanking: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
    org.flywaydb: DEBUG

# Actuator configuration for health checks and metrics
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: always
      show-components: always
      probes:
        enabled: true
  health:
    readiness-state:
      enabled: true
    liveness-state:
      enabled: true
    db:
      enabled: true
    diskspace:
      enabled: true
  metrics:
    distribution:
      percentiles-histogram:
        http.server.requests: true
      percentiles:
        http.server.requests: 0.5, 0.95, 0.99


#  management.endpoints.web.exposure.include=health, info, metrics, prometheus
#  management.metrics.distribution.percentiles-histogram.http.server.requests=true
#  management.observations.key-values.application=umms-user-service
#  management.tracing.sampling.probability=1.0