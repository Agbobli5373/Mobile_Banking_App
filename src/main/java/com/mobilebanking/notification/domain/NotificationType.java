package com.mobilebanking.notification.domain;

/**
 * Enumeration of different notification types in the system.
 */
public enum NotificationType {
    TRANSFER_SENT,
    TRANSFER_RECEIVED,
    DEPOSIT
}